prefix: "&c&l[CraftBlocker]"

prefix-enabled: true

deny-message: "&cYou cant craft this item"

crafting:
  certain-item:
    items:
      - BEACON
    enabled: false
  everything:
    enabled: true

logs:
  name: "logs"
  enabled: true
  date-format: "yyyy-MM-dd"
  content:
    format: "yyyy/MM/dd HH:mm:ss"
    
